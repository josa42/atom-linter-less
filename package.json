{
  "name": "linter-less",
  "main": "./lib/init",
  "version": "2.4.1",
  "description": "Lint `less` on the fly, using less",
  "repository": "https://github.com/josa42/atom-linter-less",
  "license": "MIT",
  "scripts": {
    "lint": "eslint lib"
  },
  "engines": {
    "atom": ">0.50.0"
  },
  "dependencies": {
    "atom-package-deps": "^4.0.1",
    "less": "^2.5.3"
  },
  "package-deps": [
    "linter"
  ],
  "providedServices": {
    "linter": {
      "versions": {
        "1.0.0": "provideLinter"
      }
    }
  },
  "devDependencies": {
    "babel-eslint": "^4.1.5",
    "eslint": "^2.1.0"
  }
}
